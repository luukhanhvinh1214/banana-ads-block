# Claude Code local config & bundled skills
.claude/

# Dependencies
node_modules/

# Build output
dist/
build/
*.zip

# Chrome sinh ra ngay trong thư mục extension khi nạp bản giải nén: bản biên
# dịch của rules/*.json. Không phải mã nguồn, và sẽ khác nhau theo từng máy.
_metadata/

# Công cụ và ghi chú chỉ dùng khi phát triển, không thuộc phần đóng gói
# extension. .mcp.json trỏ tới mcp-server/, bỏ theo luôn cho khỏi trỏ vào chỗ
# trống. CLAUDE.md giữ trên máy, không đẩy lên.
tools/
mcp-server/
test/
.mcp.json
CLAUDE.md

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
